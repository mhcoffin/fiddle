# Use pnpm for this project
# This helps avoid npm's optional dependency bugs with @rollup/rollup-darwin-arm64

# Automatically install peer dependencies
auto-install-peers=true

# Use strict peer dependencies to avoid version conflicts
strict-peer-dependencies=false

# Shamefully hoist to ensure compatibility with tools expecting node_modules structure
shamefully-hoist=true
